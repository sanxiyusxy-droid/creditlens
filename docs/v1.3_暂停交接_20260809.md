# CreditLens v1.3 暂停交接（2026-08-09）

> 目的：冻结本轮真实外部评测、问题诊断和未完成修复，供下一次继续。  
> 分支：`feat/v1.3-grounded-qa`  
> 首轮评测源码：`c9decbce96348f031a177d4a94981faf2be0062a`  
> 首轮评测归档：`86d5982 test(eval): capture initial grounded answer baseline`  
> 历史状态：本文件记录 2026-08-09 的暂停点。任务已于 2026-08-10 经用户“继续”恢复；
> 最新状态以 `README.md`、`docs/进度报告.md` 和第二轮 `c4289a1` 评测报告为准。

## 1. 已完成并冻结的外部评测

用户已批准把合成评测数据发送到已配置的外部 LLM、Embedding 和 Reranker 服务。本轮实际
完成：

1. 3 题 smoke：生成、检索、审计、citation mapping、provenance 和离线评分链路均可运行；
2. 41 题完整生成：`git_dirty=false`，源码 commit 为 `c9decbc`；
3. 首轮预测：
   `evaluation/predictions/answer_eval_v1_c9decbc_20260809T1442Z.json`；
4. 首轮报告：
   `evaluation/reports/answer_eval_v1_c9decbc_20260809T1453Z.json`；
5. 两份正式产物已由 commit `86d5982` 独立归档，未被后续修复覆盖；
6. raw checkpoint 和 3 题 smoke 位于 gitignored 路径，只作本机诊断。

首轮报告范围为 `DETERMINISTIC_LEXICAL_AND_CITATION_SET`，并明确
`semantic_entailment_evaluated=false`。

| 指标 | 首轮结果 |
|---|---:|
| 题目 | 41（30 可答、11 不可答） |
| 状态 | 24 ANSWERED / 10 REFUSED / 7 TECHNICAL_FAILURE |
| Lexical Correctness | 0/30 |
| Key-point Recall | 12.73% |
| Numeric Accuracy | 5.88% |
| Citation-set P/R/F1 | 82.76% / 72.73% / 77.42% |
| Refusal Accuracy | 8/11 = 72.73% |
| False Refusal | 0 |
| Technical Failure | 7/41 = 17.07%（6 个可答、1 个不可答） |
| Forbidden assertion | 0 |
| ambiguous / unmapped citation | 0 / 0 |

不能把 Citation F1 写成 Faithfulness，也不能把 0% lexical 写成真实问答准确率。

## 2. 7 个技术失败的已确认原因

- `q061`：初稿数字引用失败；修复稿状态/Direct Answer/结构覆盖冲突；
- `q006`、`q048`、`q118`：两轮均为 `NUMERIC_TOKEN_NOT_IN_CITATION`；
- `q050`：状态/Direct Answer 与极性冲突；
- `q102`：状态/Direct Answer 与结构覆盖不足；
- `q134`：外部模型连续两次返回不合 Pydantic Schema 的 JSON，非 HTTP/超时故障。

7 题检索均正常：`candidate_count=8`、`rerank_degraded=false`。问题属于模型结构输出、
严格审计和修复编排，不是 PostgreSQL/Qdrant/Reranker 故障。

## 3. 首轮后已写入工作树的修复

当前工作树/暂停 checkpoint 包含以下方向：

- 非阻断 structural/polarity 结果由服务端规范化为 `NEEDS_REVIEW`、
  `direct_answer=None`、`execution_status=PARTIAL`；硬完整性和数字错误仍阻断；
- 终态审计失败使用稳定安全错误码 `ANSWER_AUDIT_FAILED`；
- 审计实现 `structural_evidence_v2` 和答案契约 `1.1` 进入 QA/experiment hash；
- repair feedback 改用模型可见的 category/evidence IDs、固定 hint 和匹配范围；
- OpenAI-compatible structured retry 第二次从头生成，只携脱敏 Schema 错误位置/类型，
  Trace 的 request hash 覆盖两次真实请求，不回传首次模型原文；
- 答案 adapter 支持受限中文数字、否定作用域、数值标签桥接和旧报告兼容；
- Auditor 增加中文数字、明显否定和阈值方向检查；
- `docker-compose.test.yml` 增加独立项目名 `creditlens-test`，避免测试栈复用开发容器名。

## 4. 暂停时的验证边界

在第一批修复完成后，曾实际通过：

- Ruff check 全绿；
- Ruff format check：118 files formatted；
- 非集成：`309 passed, 19 deselected`；
- 真实 PG/Qdrant/RLS：`19 passed, 22 deselected`；
- 更早的独立 RLS verifier：21/21 PASS。

随后只读复核又发现并开始修复：

1. repair feedback 的 Claim UUID 对模型不可见；
2. Auditor 中文数字可绕过；
3. “不得超过 70%”与“已经超过 70%”方向冲突可漏检；
4. “不存在营业收入……”可同时骗过运行时 Auditor 与离线 evaluator；
5. 旧 metric 2.0 report 缺 adapter 字段时无法解析；
6. q086 的“不得低于/不低于”和 q153 的“准备金余额/准备金”仍有确定性漏分。

其中 1–5 已有代码/测试改动写入共享工作树；第 6 项在暂停前尚未完成。由于用户此时要求
暂停，**最新工作树没有重新跑完整 Ruff、全量 pytest 和真实栈**。不要把 309/19 误写成
当前最后一行代码的绿色结果。

## 5. 暂停时的本地运行状态

最后一次核验：

- 主 PostgreSQL 容器健康，迁移为 `0008_qa_request_idempotency`；
- 主库 `review_runs=79`；
- `index_outbox`：pending=0、completed=168；
- 独立评测 Qdrant `creditlens-qdrant-v13-eval` 已恢复运行，HTTP 健康端口为 6333；
- Redis、MinIO 健康；
- 旧的不兼容 Qdrant 数据卷未删除；
- 第二轮外部 3+41 **没有运行**。

## 6. 下次继续的严格顺序

1. 读取本文件、`git status`、最近提交和完整 diff；不要 reset/checkout 覆盖暂停工作树；
2. 先审查 `auditor.py`、`grounded_qa.py`、`qa_service.py`、`answer_metrics.py` 最新改动；
3. 补 q086/q153 的有界显式词法等价和反例，检查中文数字解析是否在运行时/离线一致；
4. 跑 Ruff、相关定向测试、全量非集成、真实 PG/Qdrant/RLS；
5. 只读复核没有 P0/P1 后，把修复提交为独立 commit，确保外部评测前 Git clean；
6. 再次明确外部调用授权，运行新的 3 题 smoke 和 41 题完整评测；
7. 新报告必须使用新 experiment hash，不得拼接或覆盖 `c9decbc` 首轮基线；
8. 用第二轮真实结果更新 README、进度报告、演示脚本和面试手册；
9. 最后再执行 GitHub CI、合并 `main` 和发布 `v1.3.0` 标签。

## 7. 面试手册

已同步更新：

`C:\Users\sxy\Documents\Codex\2026-07-28\wo\CreditLens_简历项目介绍与面试指南.md`

手册已包含 200 字简历简介、v1.3 架构、首轮完整指标、失败分析、Grounded QA 流程、
新增面试问答、暂停边界和续做顺序。
